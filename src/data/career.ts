import type { CareerRole } from "@/types";

// TODO (content phase): finalize company list, dates, and bullets with Hussain.
// PRD §6.2 — target 6–8 career nodes.
export const careerRoles: CareerRole[] = [];
