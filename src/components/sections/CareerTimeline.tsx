// PRD §6.2 — Career Timeline
// TODO: horizontal timeline (desktop) / vertical (mobile), hover/tap node expansion.
// Data: src/data/career.ts
export function CareerTimeline() {
  return (
    <section id="career" className="section">
      <div className="content-width">
        <h2 className="text-3xl font-semibold">Career</h2>
        <p className="mt-2 text-muted">Horizontal interactive timeline goes here.</p>
      </div>
    </section>
  );
}
