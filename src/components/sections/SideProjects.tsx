// PRD §6.4 — Side Projects (Vibe-Coded)
// TODO: compact 3-col card row, click-through to live URLs (no modal).
// Data: src/data/sideProjects.ts
export function SideProjects() {
  return (
    <section id="side-projects" className="section">
      <div className="content-width">
        <h2 className="text-3xl font-semibold">Side Projects</h2>
        <p className="mt-2 text-muted">Vibe-coded apps — quick skim and click out.</p>
      </div>
    </section>
  );
}
